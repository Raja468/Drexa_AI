"use client";
import { ArrowUpRight } from "lucide-react";
import Link from "next/link";
import { Container } from "@/components/ui/Container";
import { SectionHeader } from "@/components/ui/SectionHeader";
import { FadeUp } from "@/components/motion/FadeUp";
import { home } from "@/content/home";
export function Work() { return <section id="work" className="border-b border-border py-28 md:py-36"><Container><FadeUp><SectionHeader label={home.work.label} title={home.work.title} description={home.work.description} /></FadeUp><div className="space-y-20 md:space-y-28">{home.work.items.map((item, i) => <FadeUp key={item.slug} delay={i * .08} as="article"><Link href={item.href} className="group block"><div className={`relative overflow-hidden rounded-[18px] border border-border bg-bg-elevated ${item.featured ? "aspect-[16/8]" : "aspect-[16/7]"}`}><div className="absolute inset-0 bg-[linear-gradient(120deg,rgba(25,211,208,.08),transparent_42%,rgba(114,230,75,.05))] transition-opacity duration-300 group-hover:opacity-70" /><div className="absolute inset-[10%] border border-white/10 bg-bg-dark/60 p-5 md:p-8"><div className="flex items-center justify-between font-mono text-[10px] uppercase tracking-[.18em] text-text-muted"><span>{item.industry}</span><span>{item.number}</span></div><div className="absolute bottom-6 left-6 right-6 flex items-end justify-between md:bottom-10 md:left-10 md:right-10"><span className="max-w-[70%] font-display text-[clamp(1.4rem,3vw,2.8rem)] tracking-[-.04em] text-white/80">{item.name}</span><span className="h-10 w-10 border border-accent/40 p-2 text-accent"><ArrowUpRight className="h-full w-full" /></span></div></div></div><div className="mt-5 flex items-start justify-between gap-6"><div><h3 className="font-display text-[clamp(1.3rem,2.3vw,2rem)] tracking-[-.04em] text-white transition-colors group-hover:text-accent">{item.name}</h3><p className="mt-2 max-w-[600px] text-[15px] text-text-secondary">{item.description}</p></div><span className="hidden shrink-0 font-mono text-[10px] uppercase tracking-[.16em] text-text-muted sm:block">{item.tags.join(" · ")}</span></div></Link></FadeUp>)}</div></Container></section>; }
