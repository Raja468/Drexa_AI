"use client";
import { motion } from "framer-motion";
import { Container } from "@/components/ui/Container";
import { SectionHeader } from "@/components/ui/SectionHeader";
import { FadeUp } from "@/components/motion/FadeUp";
import { home } from "@/content/home";
export function Process() { return <section className="border-b border-border bg-bg-surface py-28 md:py-36"><Container><FadeUp><SectionHeader label={home.process.label} title={home.process.title} description={home.process.description} /></FadeUp><div className="relative grid gap-10 md:grid-cols-4 md:gap-6"><motion.div initial={{ scaleX: 0 }} whileInView={{ scaleX: 1 }} viewport={{ once: true }} transition={{ duration: 1 }} className="absolute left-[8%] right-[8%] top-5 hidden h-px origin-left bg-gradient-to-r from-accent via-white/30 to-accent-green md:block" />{home.process.steps.map((step, i) => <FadeUp key={step.number} delay={i * .08} className="relative"><div className="flex items-center gap-4 md:block"><span className="relative z-10 flex h-10 w-10 shrink-0 items-center justify-center rounded-full border border-accent bg-bg-surface font-mono text-[10px] text-accent">{step.number}</span><h3 className="font-display text-[19px] tracking-[-.03em] text-white md:mt-6">{step.title}</h3></div><p className="mt-3 pl-14 text-[15px] text-text-secondary md:pl-0">{step.body}</p><p className="mt-4 pl-14 font-mono text-[10px] uppercase tracking-[.12em] text-text-muted md:pl-0">{step.meta}</p></FadeUp>)}</div><div className="mt-20 hidden items-center justify-between border-y border-border py-4 font-mono text-[10px] uppercase tracking-[.15em] text-text-muted md:flex"><span>Problem</span><span>→</span><span>Strategy</span><span>→</span><span>Design</span><span>→</span><span>Code</span><span>→</span><span>System</span><span>→</span><span className="text-accent">Outcome</span></div></Container></section>; }
