"use client";
import { useState } from "react";
import { ChevronDown } from "lucide-react";
import { Container } from "@/components/ui/Container";
import { SectionHeader } from "@/components/ui/SectionHeader";
import { FadeUp } from "@/components/motion/FadeUp";
import { home } from "@/content/home";
export function Services() { const [active, setActive] = useState(0); return <section id="services" className="border-b border-border bg-bg-surface py-28 md:py-36"><Container><FadeUp><SectionHeader label={home.services.label} title={home.services.title} description={home.services.description} /></FadeUp><div className="border-t border-border">{home.services.items.map((item, i) => <div key={item.number} className="border-b border-border"><button type="button" aria-expanded={active === i} onClick={() => setActive(i)} className="flex min-h-[84px] w-full items-center gap-5 text-left transition-colors hover:text-accent"><span className="font-mono text-[11px] text-accent">{item.number}</span><span className="flex-1 font-display text-[clamp(1.4rem,3vw,2.5rem)] tracking-[-.04em]">{item.title}</span><ChevronDown className={`h-5 w-5 transition-transform ${active === i ? "rotate-180 text-accent" : "text-text-muted"}`} /></button><div className={`grid transition-[grid-template-rows] duration-300 ${active === i ? "grid-rows-[1fr]" : "grid-rows-[0fr]"}`}><div className="overflow-hidden"><div className="grid gap-8 pb-9 pl-9 md:grid-cols-[1fr_1fr] md:pl-16"><p className="max-w-[500px] text-[16px] text-text-secondary">{item.description}</p><div className="flex items-center gap-2 self-start pt-1 font-mono text-[10px] uppercase tracking-[.14em] text-text-muted">{item.flow.map((step, n) => <span key={step} className="flex items-center gap-2">{n > 0 && <span className="text-accent">→</span>}<span>{step}</span></span>)}</div></div></div></div></div>)}</div></Container></section>; }
