import Link from "next/link";
import { Container } from "@/components/ui/Container";
import { LogoMark } from "@/components/ui/LogoMark";
import { site, footerColumns } from "@/content/site";
export function Footer() { return <footer className="border-t border-border bg-bg-dark"><Container className="py-16 md:py-24"><div className="grid gap-14 md:grid-cols-[1.4fr_1fr_1fr]"><div><LogoMark /><p className="mt-6 max-w-[280px] text-[15px] text-text-secondary">{site.tagline}</p></div>{footerColumns.map((column) => <div key={column.title}><span className="font-mono text-[10px] uppercase tracking-[.18em] text-text-muted">{column.title}</span><ul className="mt-5 space-y-3">{column.links.map((link) => <li key={link.label}><Link href={link.href} className="text-[14px] text-text-secondary transition-colors hover:text-accent">{link.label}</Link></li>)}</ul></div>)}<div><span className="font-mono text-[10px] uppercase tracking-[.18em] text-text-muted">Contact</span><a href={`mailto:${site.email}`} className="mt-5 block text-[14px] text-text-secondary transition-colors hover:text-accent">{site.email}</a></div></div><div className="mt-16 flex flex-col justify-between gap-3 border-t border-border pt-6 font-mono text-[10px] uppercase tracking-[.14em] text-text-muted md:flex-row"><span>© {site.copyrightYear} {site.brand}</span><span>Privacy · Terms</span></div></Container></footer>; }
