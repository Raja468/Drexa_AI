"use client";
import Link from "next/link";
import { ArrowUpRight } from "lucide-react";
import { Container } from "@/components/ui/Container";
import { FadeUp } from "@/components/motion/FadeUp";
import { projects } from "@/content/work";
export default function WorkPage() { return <><section className="border-b border-border py-32 md:py-44"><Container><FadeUp><span className="mb-6 block font-mono text-[11px] uppercase tracking-[.18em] text-accent">Selected work</span><h1 className="max-w-[800px] font-display text-[clamp(3rem,7vw,6rem)] font-medium leading-[.95] tracking-[-.06em] text-white">Projects, not promises.</h1><p className="mt-8 max-w-[560px] text-[17px] text-text-secondary">A selection of product, AI, and engineering work. Detailed case study material will be added as it is verified.</p></FadeUp></Container></section><section className="border-b border-border bg-bg-surface py-28 md:py-36"><Container><div className="space-y-24">{projects.map((project, i) => <FadeUp key={project.slug} as="article"><div className="relative aspect-[16/8] overflow-hidden rounded-[18px] border border-border bg-bg-elevated"><div className="absolute inset-[10%] border border-white/10 bg-bg-dark/70 p-5 md:p-8"><div className="flex justify-between font-mono text-[10px] uppercase tracking-[.16em] text-text-muted"><span>{project.industry}</span><span>0{i + 1}</span></div><span className="absolute bottom-6 left-6 max-w-[70%] font-display text-[clamp(1.5rem,4vw,3.5rem)] tracking-[-.05em] text-white/75 md:bottom-10 md:left-10">{project.name}</span></div></div><div className="mt-7 grid gap-6 md:grid-cols-[1fr_auto]"><div><h2 className="font-display text-[clamp(1.5rem,3vw,2.4rem)] tracking-[-.04em] text-white">{project.name}</h2><p className="mt-3 max-w-[580px] text-[15px] text-text-secondary">{project.description}</p></div><div className="font-mono text-[10px] uppercase tracking-[.14em] text-text-muted">{project.tags.join(" · ")}</div></div></FadeUp>)}</div></Container></section><section className="py-28 md:py-36"><Container><FadeUp><h2 className="font-display text-[clamp(2.5rem,5vw,4rem)] font-medium leading-none tracking-[-.05em] text-white">Have an idea worth building?</h2><Link href="/contact" className="mt-8 inline-flex h-12 items-center gap-2 rounded-[9px] bg-white px-6 text-[14px] font-semibold text-bg-dark hover:bg-accent">Start a project <ArrowUpRight className="h-4 w-4" /></Link></FadeUp></Container></section></>; }
