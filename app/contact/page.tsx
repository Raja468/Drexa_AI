"use client";
import { useState } from "react";
import { ArrowUpRight } from "lucide-react";
import { Container } from "@/components/ui/Container";
import { FadeUp } from "@/components/motion/FadeUp";
import { site } from "@/content/site";
export default function ContactPage() { const [status, setStatus] = useState<"idle" | "sending" | "sent" | "error">("idle"); async function handleSubmit(e: React.FormEvent<HTMLFormElement>) { e.preventDefault(); setStatus("sending"); const form = e.currentTarget; const data = Object.fromEntries(new FormData(form)); try { const res = await fetch("/api/contact", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(data) }); if (!res.ok) throw new Error("Failed"); setStatus("sent"); form.reset(); } catch { setStatus("error"); } } const inputClass = "w-full min-h-12 rounded-[9px] border border-border bg-bg-elevated px-4 text-base text-white placeholder:text-text-muted focus:border-accent focus:outline-none focus:ring-2 focus:ring-accent/20"; return <><section className="border-b border-border py-32 md:py-44"><Container><FadeUp><span className="mb-6 block font-mono text-[11px] uppercase tracking-[.18em] text-accent">Start a project</span><h1 className="max-w-[800px] font-display text-[clamp(3rem,7vw,6rem)] font-medium leading-[.95] tracking-[-.06em] text-white">Have an idea worth building?</h1><p className="mt-8 max-w-[560px] text-[17px] text-text-secondary">Tell us what you&apos;re trying to solve. We&apos;ll help turn the idea into a clear plan, and a product built to work.</p></FadeUp></Container></section><section className="bg-bg-surface py-28 md:py-36"><Container><div className="grid gap-16 lg:grid-cols-[1.2fr_.8fr] lg:gap-24"><FadeUp>{status === "sent" ? <div className="border border-accent/30 bg-accent/5 p-8"><h2 className="font-display text-2xl text-white">Message sent.</h2><p className="mt-2 text-text-secondary">We&apos;ll get back to you as soon as possible.</p></div> : <form onSubmit={handleSubmit} className="space-y-6"><div className="grid gap-6 sm:grid-cols-2"><label className="block"><span className="mb-2 block text-[13px] text-white">Name</span><input name="name" required className={inputClass} /></label><label className="block"><span className="mb-2 block text-[13px] text-white">Email</span><input name="email" type="email" required className={inputClass} /></label></div><label className="block"><span className="mb-2 block text-[13px] text-white">Company</span><input name="company" className={inputClass} /></label><label className="block"><span className="mb-2 block text-[13px] text-white">What are you building?</span><textarea name="message" rows={6} required className={`${inputClass} resize-none py-3`} /></label><button type="submit" className="inline-flex h-12 items-center gap-2 rounded-[9px] bg-white px-6 text-[14px] font-semibold text-bg-dark hover:bg-accent">{status === "sending" ? "Sending" : "Start a project"}<ArrowUpRight className="h-4 w-4" /></button>{status === "error" && <p className="text-[14px] text-red-400">Something went wrong. Please try again or email us directly.</p>}</form>}</FadeUp><FadeUp delay={.1}><span className="font-mono text-[11px] uppercase tracking-[.18em] text-accent">Email</span><a href={`mailto:${site.email}`} className="mt-4 block text-xl text-white hover:text-accent">{site.email}</a></FadeUp></div></Container></section></>; }
